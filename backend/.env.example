# 服务器配置
PORT=3000
NODE_ENV=development

# 数据库配置 (SQLite - 文件数据库，无需账号密码)
# 路径相对于项目根目录
DB_PATH=database/azothpath.db

# JWT 密钥 (用于用户登录 token 签名)
JWT_SECRET=your_jwt_secret_key_change_this_in_production
JWT_EXPIRES_IN=7d

# 外部验证 API（用于验证配方是否在游戏中有效）
EXTERNAL_API_URL=https://api.example.com/validate
